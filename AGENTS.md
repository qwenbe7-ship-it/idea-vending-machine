# AGENTS.md — 아이디어 자판기 개발 규칙

이 저장소에서 작업하는 AI/개발자는 아래 규칙을 우선 적용한다.

## 1. Raw idea에서 바로 코딩 금지
아이디어는 먼저 문제, 사용자, 입력, 출력, 제약, 위험, 성공조건으로 구조화한다.

## 2. Acceptance Criteria 선행
새 기능은 구현 전에 관찰 가능한 성공/실패 조건을 정의한다.

## 3. AI 판단과 deterministic logic 분리
재현 가능해야 하는 검증·권한·상태전이는 결정론적으로 구현한다. AI 판단은 교체 가능한 경계 뒤에 둔다.

## 4. TDD
동작 변경은 실패하는 테스트를 먼저 확인한 뒤 최소 구현으로 통과시킨다.

## 5. Browser-facing 기능은 E2E 검증 대상
UI 흐름은 단위 테스트만으로 완료 처리하지 않는다. 실제 브라우저 E2E를 추가할 수 있는 구조를 유지한다.

## 6. Security-sensitive 기능은 별도 검토
인증, 결제, 개인정보, 파일 업로드, 외부 명령 실행이 추가될 경우 보안 검토와 실패 케이스를 먼저 설계한다.

## 7. NO GREEN WITHOUT EVIDENCE
"동작할 것 같다", "코드상 문제없다", "완성했다"는 완료 근거가 아니다.

완료 전 최소 조건:

```text
python scripts/verify.py
```

이 명령이 실제로 0으로 종료되고 `GREEN WITH EVIDENCE`를 출력해야 한다.

## 8. 실패하면 릴리스 차단
테스트, 컴파일, 보안 계약 중 하나라도 실패하면 배포/머지를 진행하지 않는다.

## 9. 범위 확대 금지
현재 작업의 Acceptance Criteria와 직접 관계없는 리팩터링·기능추가는 별도 제안으로 분리한다.

## 10. 결과보다 검증 가능한 결과물
생성된 코드보다 테스트 결과, 재현 명령, 실패 복구 경로를 함께 남긴다.
